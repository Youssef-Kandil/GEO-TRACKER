services:
  geo-tracker:
    build: .
    image: geo-tracker:latest
    container_name: geo-tracker
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data.json:/app/data.json
